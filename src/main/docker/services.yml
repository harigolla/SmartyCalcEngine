# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: smartycalcengine
services:
  postgresql:
    extends:
      file: ./postgresql.yml
      service: postgresql
  memcached:
    extends:
      file: ./memcached.yml
      service: memcached
